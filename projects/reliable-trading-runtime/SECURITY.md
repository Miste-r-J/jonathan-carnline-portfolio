# Security and Public Data

This public repository is for source review. Do not commit broker credentials, account identifiers, webhook URLs, private keys, access tokens, raw run logs, private datasets, trained model artifacts, or machine-specific configuration.

The sample configuration is not authorization to run against a live account. Anyone evaluating the code should use isolated test data and paper or replay modes.

