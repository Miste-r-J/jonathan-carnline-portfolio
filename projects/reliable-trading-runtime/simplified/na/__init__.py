"""
Simplified ``na`` package used for the standalone training/streaming gym.

This package intentionally mirrors the top-level module name from the full
trading stack so existing imports keep working.  To run the gym, either
``cd simplified`` before invoking Python or set ``PYTHONPATH=./simplified`` so
this lightweight package resolves ahead of the production one.
"""
